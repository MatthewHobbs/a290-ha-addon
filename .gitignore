# macOS / tooling
.DS_Store
._*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ruff_cache/

# Test / coverage artifacts
.coverage
htmlcov/
.pytest_cache/

# Secrets / local env (the add-on takes credentials via its config page)
secrets.yaml
.env
*.local

# Claude Code — personal/local settings only (CLAUDE.md and any shared .claude/ config stay tracked)
.claude/settings.local.json

# Editor / IDE
.vscode/
.idea/
